# extensions-store  m1

version: '3'
networks:
    default:
        external:
           name: magento
services:
    nginx: 
      image: nginx:alpine
      container_name: m1_es_nginx
      volumes:
        - "./www:/var/www/m1.extensions-store.com"
        - "./etc/ssl:/etc/ssl"
        - "./etc/nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf"
      expose:
        - "80"
      environment:
        VIRTUAL_HOST: m1.extensions-store.com
      restart: always
      depends_on:
        - php
    php: 
      image: m1_es/php
      container_name: m1_es_php
      ports: 
        - "19000:9000"
      volumes:
        - "./etc/php/php.ini:/usr/local/etc/php/conf.d/php.ini"
        - "./etc/php/www.conf:/usr/local/etc/php-fpm.d/www.conf"
        - "./www:/var/www/m1.extensions-store.com" 


    